# Contact form — forwards submissions to this external endpoint (Resend, Formspree, etc.)
# Leave empty to disable forwarding (the API route will return 500 for unconfigured requests).
CONTACT_ENDPOINT=https://formspree.io/f/YOUR_FORM_ID
